apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chuzone-ingress
  namespace: examen-26
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: firas997.duckdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chuzone-service
                port:
                  number: 80
